/**
 * scraply init
 * scraply new --name=CustomCrawler
 * scraply publish --crawler CustomCrawler --cached "10m"
 * scraply execute CustomCraler -a url="" -a
 * scraply enqueue <LIKE `scraply call`>
 * scraply flush --queue=true --archive=true
 * scraply serve
 */
